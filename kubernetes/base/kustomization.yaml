apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mobilebank

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: mobilebank-api
  managed-by: devops-lab
