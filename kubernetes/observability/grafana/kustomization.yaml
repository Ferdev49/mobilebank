apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: observability

resources:
  - secret.yaml
  - datasources.yaml
  - dashboard-providers.yaml
  - dashboard-mobilebank.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  managed-by: devops-lab
  week: "4"
  day: "19"
